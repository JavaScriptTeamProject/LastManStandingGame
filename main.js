(function() {	
	var canvas = document.getElementById("canvas-animation");
	var ctx = canvas.getContext("2d");
	//Canvas for the objects we will animate later on.

	var background = new Background();
	background.draw();
}());